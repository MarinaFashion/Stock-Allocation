[project]
name = "stock_auto_allocation"
authors = [
    { name = "Your Company", email = "dev@yourcompany.com" }
]
description = "Stock Auto-Allocation Module for ERPNext — DC-to-store and store-to-store allocation based on sales velocity and coverage days."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
erpnext = ">=15.0.0,<16.0.0"

[tool.bench.dev-dependencies]
